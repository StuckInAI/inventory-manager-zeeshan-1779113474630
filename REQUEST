Need full file contents of AtsContext, types, and all pages mentioned in errors to fix them. Without these I can only guess. Below are best-effort fixes based on error messages alone.

This file is a placeholder request — see other files.